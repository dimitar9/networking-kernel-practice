1. install libmnl-dev on ubuntu.
2. find libmnl.so place: dpkg -L libmnl-dev
3. Testing method: 
  a) make
  b) sudo insmod nlexample2.ko
  c) make userspace
  d) ./event
  e) ./change

  I can use "strace" to track event and change. see system call's hierachy
